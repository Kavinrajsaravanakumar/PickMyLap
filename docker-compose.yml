version: '3'

services:

  frontend:
    build: ./client
    ports:
      - "3000:80"

  backend:
    build: ./server
    ports:
      - "5000:5000"
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    ports:
      - "27017:27017"